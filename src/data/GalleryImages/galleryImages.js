import {
  grejanje,
  klimatizacija,
  ventilacija,
  servisKlimaImages,
  kernovanjeImages,
} from './allGalleryImages'

const galleryImages = {
  grejanje,
  klimatizacija,
  ventilacija,
  servisKlimaImages,
  kernovanjeImages,
}

export default galleryImages
